<Window x:Class="Parcheggio.Views.IncassoAttuale"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Parcheggio.Views"
        mc:Ignorable="d"
        Title="Incasso Attuale" Height="300" Width="335">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="4.8"></RowDefinition>
            <RowDefinition Height="182*"></RowDefinition>
        </Grid.RowDefinitions>
        <DockPanel Margin="35,2,35,3" Grid.Row="1">
            <DataGrid x:Name="Incasso_Giornaliero" AutoGenerateColumns="False" CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" DockPanel.Dock="top">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Incasso" Width="150" Binding="{Binding IncassoTotale}"/>
                    <DataGridTextColumn Header="Parcheggio" Width="150" Binding="{Binding NomeParcheggio}"/>

                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>
